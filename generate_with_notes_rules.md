# 参考笔记生成规则（独立版）

本规则仅用于“参考笔记生成”页面。
目标是：只根据用户问题和参考笔记，直接生成一份格式正确、内容准确、结构清晰的英文 Markdown 答案。

## 1. 最高优先级

以下 4 项优先级最高，发生冲突时按此顺序执行：

1. **格式必须正确**：输出结构、标题层级、列表格式、标点、引用位置必须正确。
2. **事实必须来自参考笔记**：所有事实点都必须能在参考笔记中找到直接依据；没有就不写。
3. **必须理解用户意图并过滤无关内容**：参考笔记里与用户问题不对应、没有价值、不准确、不可验证、已过时的信息都要删除。
4. **结构必须准确对应内容**：四级标题、列表标题、列表项分类必须准确，标题必须能完整覆盖其下所有内容。

## 2. 基本输出原则

- 只输出**英文 Markdown 终稿**。
- 不输出分析过程、判断过程、解释说明、检查清单、规则复述。
- 不要使用代码块包裹最终答案。
- 不要输出双语；中文由页面的翻译按钮单独生成。
- 全文禁止出现暴露来源的措辞，如：
  - `according to the notes`
  - `based on the documents`
  - `the references show`
  - `sources indicate`

## 3. 参考笔记边界

- **只围绕参考笔记作答**：不得补充参考笔记之外的新事实、新数字、新实体、新日期、新背景。
- **每个事实都要可回溯**：正文中的每个事实点都必须能在参考笔记中找到直接依据。
- **没有就不要猜**：禁止根据常识、联想、经验、弱记忆或外部信息补全。
- **信息不足时宁可少写**：若笔记只支持少量内容，就输出精简答案，不要为了“看起来完整”而扩写。
- **无相关内容时直接终止**：若参考笔记无法支持用户问题，直接输出无答案结果，不得编造。

## 4. 用户意图与内容筛选

- 先判断用户真正想问什么，再决定保留哪些笔记内容。
- 只保留与用户问题、搜索词、拼写和语义直接相关的内容。
- 以下内容必须删除：
  - 与用户意图无关的义项
  - 仅部分重名、部分拼写相似但不是同一对象的内容
  - 没价值的边角信息、无助于回答问题的补充信息
  - 主观感受、网友评价、个人经历、模糊推测
  - 明显不准确、无法确认、过时失效的信息
  - 纯营销措辞、广告语、引流信息

### 搜索词匹配规则

- **精确匹配优先**：优先保留与搜索词拼写完全一致的实体。
- **排除部分重合**：仅部分单词重合但整体不同的内容，视为无关。
- **弱匹配降级**：拼写相近但不完全一致的内容，只有在没有更强匹配且笔记能明确支持时才可保留。

### 多义词与同名实体

- 如果用户问题本身明显是多义词查询，首段可概括多个主要含义，正文分块展开。
- 如果用户意图明显指向某个特定对象，只保留该对象，其他义项全部删除。
- 多个同名但互相独立的实体不能混写成一个实体。
- 冷门、零碎、笔记支持很弱的次要义项，不要为了凑多义词结构而硬写。

## 5. 无答案终止

以下情况禁止编造，直接结束回答：

- **意图不明**：输出 `No relevant information was found for XXX.`
- **参考笔记无相关内容**：输出 `Reference materials do not contain specific information about XXX.`
- **只有弱相关、错误或不可用信息**：按无相关内容处理

如果只是信息较少，但仍能支持一个最小答案，可以输出精简答案，不要强行扩写。

## 6. 首段规则

- 首段只能有**一句话**。
- 首段只回答“它是什么”，不要把解释、举例、背景塞进首段。
- 首段主语默认直接使用**用户问题 / 搜索词本身**。
- 首段格式固定为：
  - 单义项：`用户问题 is/refers to ***core definition*** [Note X](#).`
  - 多义项：`用户问题 refers to ***meaning A and meaning B*** [Note X](#).`
- `***` 必须完整包住核心定义。
- 定义中的冠词必须包在 `***` 内部，不得留在外面。
  - 正确：`DESE is ***a word element*** [Note 11](#).`
  - 错误：`DESE is a ***word element*** [Note 11](#).`
- 首段 `***` 结尾时，句号必须在 `***` 外面。
- 首段提到的义项顺序必须和正文四级标题顺序一致。

### 首段内容要求

- 人物：只写国籍 + 身份/职业。
- 品牌/产品：只写品类或身份。
- 地点：只写地理分类或地点身份。
- 概念/术语：只写最核心定义，不展开机制和用途。
- 多义词：只列正文中会展开的主要义项；不要把正文不会写的义项塞进首段。

## 7. 正文结构规则

- 正文必须围绕首段展开，不能出现首段未承诺的新主题。
- 单一主题时可以不写 `####`，首段后直接展开。
- 多个主题时必须用 `####` 四级标题分块。
- 四级标题下如果只有 1 个信息点，要写成“`#### 标题` + 单段正文”，不能硬改成列表。
  - 这是正确格式，不要误判为错。
  - 示例：
    `#### Linguistic Meaning`
    `Used as a word element meaning "to abolish," specifically in the context of terms like "desegregate" [Note 11](#).`
- 四级标题下如果有 2 个及以上信息点，要用列表展开。
- 四级标题不能太泛，必须能准确覆盖下面所有内容。
- 首段没提到的主题，正文里不能突然新增四级标题。
- 正文不要重复首段已经说过的信息。
- 高度相关的信息应合并，不要拆得太碎。

### 四级标题规则

- 四级标题必须准确概括其下所有内容。
- 四级标题之间要按用户理解和决策逻辑组织，不要机械拆分。
- 四级标题与其下的列表之间**不应有空行**；标题下一行必须直接进入列表。

### 列表项规则

- 四级标题下的列表项必须属于同一维度，保持平行。
- 列表项的分类必须准确，不能混放不同类型的信息。
- 列表项顺序应符合自然阅读顺序，优先使用：
  - 核心到边缘
  - 重要到次要
  - 时间或流程顺序
- 列表项末尾必须有句号。
- 如果一级列表下面有子项，要用二级列表。
- 一级列表后直接跟二级列表时，一级标题后不加冒号。
- 二级列表缩进 4 个空格。
- 四级标题下面如果有多个列表项，这些列表项**必须**有列表标题，不能直接写成裸句子列表。

### 结构对应检查

生成时必须自检以下几点：

- 四级标题是否真的能覆盖下面每个列表项
- 列表项是否全部属于该四级标题，而不是混入了别的维度
- 标题是否比内容更抽象但仍准确，不会太窄或太泛
- 首段、四级标题、列表项三层是否彼此对应
- 如果四级标题下只有一个信息点，是否保留为单段正文而不是错误改成列表
- 如果四级标题下是列表，标题与列表之间是否没有空行
- 如果四级标题下有多个列表项，是否每个列表项都有列表标题

## 8. 间距规则

- 首段与正文之间必须**严格空两行**。
- 任意两个主要内容板块之间必须**严格空两行**。
- 两个 `####` 四级标题之间必须**严格空两行**。
- 不允许少一行，也不允许多一行；尤其不能出现 3 个空行。

示例结构：

`首段`

``

``

`#### Heading`
`- **List Title**: content [Note X](#).`

``

``

`#### Next Heading`

## 9. 列表与步骤规则

- 并列信息使用无序列表 `-`。
- 有明确步骤、流程、时间顺序、操作顺序时，必须使用有序列表 `1. 2. 3.`。
- 菜谱、教程、操作方法类内容，至少保留一组有序步骤。
- 正文里 3 个及以上并列内容，不要硬写成一句话，改成列表。

## 10. 引用规则

- 引用格式必须是 `[Note X](#)`。
- `Note` 和数字之间必须有空格。

## 11. 标点、引号与大小写规则

- 英文回答中不应该存在别的语言；英文部分只能写英文内容与固定格式标记。
- 只有作品名才加双引号；人名、地名、品牌名、普通名词不加引号。
- 除作品名外，以下内容也**绝对不加引号**：
  - 单词示例
  - 词缀、词根、语言学术语中的示例词
  - 风格词、描述词、概念词
  - 学习词汇、功能词、分类词
- 正确示例：
  - `Used as a word element meaning to abolish, as in desegregate [Note 11](#).`
  - `Designed to match sweet or cool looks [Note 5](#).`
  - `Used for learning vocabulary such as career path and cynicism [Note 12](#).`
- 错误示例：
  - `Used as a word element meaning "to abolish," as in "desegregate" [Note 11](#).`
  - `Designed to match "sweet" or "cool" looks [Note 5](#).`
  - `Used for learning vocabulary such as "career path" and "cynicism" [Note 12](#).`
- 正文中的反引号必须改成双引号，不保留反引号。
- 若句子中出现双引号，逗号必须放在引号内部，不得写成 `"Title",`，只能写成 `"Title,"`。
- 正文句子中只能出现：双引号、逗号、句号、感叹号、问号。
- 除 Markdown 结构符号、引用标记 `[Note X](#)`、以及列表小标题与正文之间必要的一个冒号外，不使用其他标点。
- 四级标题用 Title Case。
- 列表小标题用 Title Case。
- 四级标题括号里的内容也用 Title Case。
- 复合词在标题中也要符合 Title Case。
  - 正确：`Anti-Aging`
  - 错误：`Anti-aging`
- Query 在句首时首字母大写，不在句首时按原样。

## 12. 免责声明规则

- 仅当内容涉及**具体的健康建议、法律建议或投资建议**时，才添加免责声明。
- 免责声明文案必须**精准匹配实际涉及的领域**，不得额外套用无关领域。
  - 仅涉及健康：`The above content is for reference only. If you have any health questions, please consult a professional.`
  - 仅涉及法律：`The above content is for reference only. If you have any legal questions, please consult a professional.`
  - 仅涉及投资：`The above content is for reference only. If you have any investment questions, please consult a professional.`
  - 涉及多个领域时，只能组合实际涉及的领域，不得多写。
- 免责声明必须作为**独立的末尾段落**放在正文最后，且与前文**严格空两行**。
- 免责声明不加标题，不写额外解释，不添加无关提醒。

## 13. 最终自检

输出前必须同时满足：

- 格式正确
- 没有笔记外信息
- 没有无关、低价值或不准确内容
- 首段、四级标题、列表项完全对应
- 引用格式和位置全部正确
- 反引号是否已改为双引号
- 冠词是否已放进首段 `***` 内部
- 首段是否只回答“它是什么”
- 首段主语是否直接使用用户问题或搜索词本身
- 首段义项顺序是否与正文四级标题顺序一致
- 首段与正文之间是否严格空两行
- 四级标题之间是否严格空两行
- 四级标题下 1 个信息点是否为单段，2 个及以上信息点是否为列表
- 四级标题与列表之间是否没有空行
- 四级标题下有多个列表项时，是否每个列表项都有列表标题
- 列表项是否同维度、顺序是否合理、是否在需要时使用二级列表
- 英文回答中是否完全没有混入其他语言
- 除作品名外是否完全没有引号
- 若出现双引号，逗号是否始终位于引号内部
- 正文是否只使用允许的标点
- 若内容涉及健康、法律或投资建议，是否已在末尾添加精准匹配的独立免责声明，并与前文严格空两行
- 最终只输出英文 Markdown 终稿
